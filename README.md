# 🍴 Canteen VWA

Hệ thống quản lý Canteen VWA — React + Node/Express + MongoDB.

## 🚀 Chạy nhanh

### Cách 1: Local (không Docker)

```bash
npm install
cp .env.example .env
# Điền MONGO_URI + JWT_SECRET vào .env
npm run dev
# Mở http://localhost:5173